{
  "_comment": "Reference configuration for the two MCP servers used by this workflow. The GitHub Actions workflow writes this config to ~/.copilot/mcp-config.json on the self-hosted runner at runtime, substituting secrets. This file is NOT read directly by any tooling — it is documentation only.",
  "mcpServers": {
    "jira-remote": {
      "type": "http",
      "url": "https://mcp.atlassian.com/v1/mcp",
      "headers": {
        "Authorization": "Basic ${ATLASSIAN_BASIC_AUTH}"
      },
      "env": {
        "ATLASSIAN_BASE_URL": "https://parasoft-demo.atlassian.net"
      }
    },
    "virtualize": {
      "type": "http",
      "url": "${VIRTUALIZE_MCP_URL}",
      "headers": {
        "Authorization": "Basic ${VIRTUALIZE_AUTH_TOKEN}"
      }
    }
  }
}
